# Fixoria — Design Brief (Phase 2–6)

هذا الملف موجّه لأي شخص (أو جلسة Claude Code) سيكمل **مرحلة التصميم والتنسيق الشاملة** لمشروع Fixoria بعد أن اكتملت كل الوظائف. اقرئي هذا الملف كاملًا قبل البدء بأي تعديل.

---

## 1. عن المشروع

Fixoria منصة تربط سكان **مجمع البدور السكني** بحرفيين/مزوّدي خدمات صيانة محليين. ثلاثة أدوار: **ساكن (Resident)**، **حرفي (Artisan)**، **مشرف (Admin)**. الواجهة الأمامية فقط (React)، والباك إند سيُبنى لاحقًا من فريق آخر — البيانات حاليًا وهمية (Mock) مخزّنة في `localStorage` عبر طبقة خدمات (`src/services/*.js`) بواجهة تُحاكي API حقيقي (كلها تعيد Promise).

**اللغة:** عربي بالكامل، RTL (`<html lang="ar" dir="rtl">`). كل النصوص في `src/i18n/ar.js` عبر دالة `translate('key.path')` — **لا تكتبي أي نص عربي مباشرة داخل الصفحات**. تبديل اللغة (عربي/إنجليزي) **لم يُبنَ بعد** ومطلوب في هذه المرحلة (انظر القسم 7).

**الحالة الوظيفية:** المشروع **مكتمل وظيفيًا 100%** (Phase 1 من خارطة الطريق) — تصفح، تسجيل، طلب خدمة، تتبّعه، تقييمه (الساكن)، تسجيل وقبول طلبات وتحديث حالتها وملف شخصي وتقييمات (الحرفي)، وإحصائيات وموافقة حرفيين وإدارة طلبات وتصنيفات وتقييمات (المشرف). **مهمتك التصميم والهيكلة فقط، وليس إضافة ميزات جديدة** إلا ما هو مذكور صراحة هنا (مثل Footer وتبديل اللغة).

---

## 2. المكدّس التقني الحالي (لا تغيّريه بلا سبب)

- **React + Vite** (JavaScript عادي، بدون TypeScript)
- **React Router** (`react-router`, الإصدار 8) — لاحظي أنه `react-router` وليس `react-router-dom`
- **Tailwind CSS v4** عبر `@tailwindcss/vite`، مضاف حديثًا واستُخدم فقط في مكوّن `ServiceCard` حتى الآن. **مستورد بدون Preflight عمدًا** (`@import "tailwindcss/theme.css" layer(theme); @import "tailwindcss/utilities.css" layer(utilities);` في `src/styles/tailwind.css`، بلا `preflight.css`) حتى لا يُعاد ضبط عناصر HTML في الصفحات القديمة (CSS عادي) بشكل غير مقصود. إن احتجتِ Preflight لاحقًا فعّليه بحذر وتأكدي من عدم كسر الصفحات القديمة.
- **lucide-react** لأيقونات SVG
- **motion** (حزمة npm اسمها `motion`, استيراد `from 'motion/react'`) للحركات — **وليس** `framer-motion`
- بقية الصفحات (كل شيء عدا `ServiceCard`) بـ **CSS عادي**، ملف `.css` مستقل بجانب كل صفحة/مكوّن، يعتمد على متغيرات CSS موحّدة (القسم التالي)

**لا تضيفي مكتبة جديدة** (مخطط بياني، مكتبة أيقونات أخرى، إلخ) دون تبرير واضح لماذا Tailwind + lucide + motion لا تكفي.

---

## 3. نظام الألوان (Design Tokens)

**ملاحظة مهمة من صاحبة المشروع:** الهوية اللونية والألوان **مفتوحة تمامًا للتغيير** حسب ذوقك التصميمي — اللوحة أدناه هي ما استُخدم إلى الآن، وليست قيدًا نهائيًا. غيّريها بحرية إن رأيتِ هوية أفضل تناسب Fixoria.

كل الموقع (عدا صفحة البوابة `/` وبطاقات الخدمة) يعتمد على متغيرات CSS معرّفة في `src/index.css` تحت `:root`. **هذا أهم شيء يجب فهمه (بغضّ النظر عن الألوان المُختارة):** لتغيير الهوية اللونية لكل الموقع دفعة واحدة، تُعدَّل هذه المتغيرات فقط في مكان واحد، وليس كل ملف على حدة — هذا ينطبق على أي ألوان تختارينها، فقط حدّثي القيم هنا وستنتشر تلقائيًا في كل الصفحات (عدا البوابة وبطاقات الخدمة المستقلتين تصميميًا).

```css
:root {
  --color-bg: #fbf6f3;        /* خلفية الصفحات */
  --color-surface: #ffffff;   /* خلفية البطاقات */
  --color-dark: #263056;      /* كحلي: الشريط العلوي، النصوص الأساسية */
  --color-dark-soft: #3a4570;
  --color-accent: #f19035;    /* برتقالي: الأزرار الأساسية، الروابط النشطة */
  --color-accent-soft: #fdb78e;
  --color-on-dark: #fdf3ee;   /* نص فاتح فوق خلفية داكنة */
  --color-text: #263056;
  --color-text-muted: #8b7b76;
  --color-border: #ead9d1;
  --color-error: #c62828;
}
```

**لوحة العلامة التجارية المستخدمة حتى الآن (نقطة انطلاق، وليست إلزامية):**

| اللون | القيمة | الاستخدام النموذجي |
|---|---|---|
| كحلي عميق | `#263056` | نص أساسي، خلفيات داكنة، ثقة/احترافية |
| برتقالي | `#F19035` | اللون المميز الأساسي (CTA، حالة نشطة) |
| خوخي | `#FDB78E` | لون ثانوي دافئ |
| بني/وردي باهت | `#AA8173` | لون ثالث، نصوص ثانوية مشتقة منه |
| بيج فاتح | `#DAC7C0` | حدود، خلفيات فاتحة دافئة |

استخدميها كما هي، أو عدّليها، أو استبدليها بلوحة مختلفة كليًا إن رأيتِ ذلك أفضل لهوية Fixoria — القرار لكِ. المهم فقط أن تبقى اللوحة النهائية (أيًا كانت) **متّسقة ومحدودة العدد** عبر كل الموقع (عبر المتغيرات في `src/index.css`)، لا ألوان عشوائية متناثرة بلا نظام. شارات حالة الطلب (Open/Accepted/In progress/Completed/Cancelled) تستخدم حاليًا ألوانًا دلالية مستقلة (أزرق/أصفر/أخضر/رمادي) وليست من هذه اللوحة عمدًا — هذا مقصود (ألوان الحالة عالمية الدلالة)، ويُفضَّل إبقاؤها كذلك ما لم يكن هناك سبب تصميمي قوي لتغييرها.

**الخط:** Tajawal (Google Fonts)، محمّل في `index.html`، ومُفعَّل في `body` بـ `src/index.css`.

---

## 4. خارطة الصفحات والأدوار

```
/                          البوابة (Landing) — بلا Layout، خلفية صورة، أزرار الأدوار الأربعة
/home, /artisans, /artisans/:id, /requests/new/:id,
/my-requests, /my-requests/:id                 → PublicLayout (شريط علوي، الساكن + الزائر)
/artisan/pending, /artisan/requests,
/artisan/my-work, /artisan/reviews,
/artisan/profile                               → ArtisanLayout (شريط علوي داكن)
/admin/dashboard, /admin/artisans,
/admin/requests, /admin/categories,
/admin/reviews                                 → AdminLayout (Sidebar داكن)
/login, /artisan/login, /admin/login,
/register, /artisan/register                   → بلا Layout، بطاقة مركزية
```

**نقطة مهمة جدًا لهذه المرحلة (من طلب المستخدم الأصلي):** اللوحات الثلاث (الساكن/الحرفي/المشرف) **يجب ألا تبدو كأنها نفس القالب بصلاحيات مختلفة**. لكل دور غرض مختلف تمامًا:
- **الساكن:** اكتشاف + خدمات + طلبات (Discovery)
- **الحرفي:** مساحة عمل تجارية (Business workspace) — بطاقات إحصائية ذات معنى (طلبات جديدة، نشطة، مكتملة، متوسط التقييم)، نشاط حديث، إجراءات سريعة
- **المشرف:** مركز عمليات المنصة (Operations center) — إحصائيات شاملة، وحيث يفيد استخدام رسوم بيانية خفيفة (مكتبة رسوم بيانية بسيطة إن كانت مفيدة فعلًا، اشرحي السبب أولًا)

---

## 5. الحركة (Motion) — قواعد صارمة

- استخدمي `motion` (`motion/react`) للحركات الأساسية: دخول العناصر (fade + translate)، تأثيرات hover، تتابع (stagger) للقوائم/البطاقات.
- **احترمي `prefers-reduced-motion`** — هذا **غير مُطبَّق حاليًا في أي مكان بالموقع**، ويجب إضافته في هذه المرحلة (سواء عبر media query في CSS أو فحص في JS قبل تفعيل حركات motion).
- ممنوع: قفز/دوران/حركة مستمرة لا تتوقف، أو حركات مبالغ فيها تُشتت عن المحتوى.
- حركات مطلوبة تحديدًا: دخول الصفحة الرئيسية (Hero) بتتابع (عنوان ← نص فرعي ← زر)، بطاقات الخدمات (موجودة جزئيًا)، تفاعل الأزرار (scale خفيف + انتقال أيقونة)، الشريط العلوي، كشف الأقسام عند التمرير (Scroll reveal).

---

## 6. الهوية البصرية النهائية (من طلب المستخدم الأصلي — بالحرف تقريبًا)

الهدف: يجب ألا يبدو Fixoria كـ"مشروع طالب" أو "قالب Tailwind افتراضي" أو "لوحة CRUD عامة" أو "مجموعة بطاقات بيضاء عشوائية". يجب أن يبدو كمنتج Startup حقيقي وحديث.

**الإحساس المطلوب:** ثقة، احترافية، موثوقية، خدمات منزلية، مجتمع محلي، حرفيون ماهرون، تقنية حديثة، ودود لكن راقٍ (Premium)، مستقبلي قليلًا (Slightly futuristic)، لا يُنسى.

**مسموح ومُشجَّع (بحكمة، ليس في كل عنصر):**
- Glassmorphism (أسطح زجاجية شفافة + Backdrop blur) — **مع تسلسل هرمي بصري، ليس كل عنصر زجاجيًا**
- تدرجات لونية ناعمة (Gradients) من لوحة الألوان النهائية التي تختارينها
- توهج خفيف خلف البطاقات (Glow)، حدود متدرجة (Gradient borders)
- عناصر ثلاثية الأبعاد/عمق خفيفة (3D-inspired)، Parallax خفيف حيث يفيد — **ليس تجربة 3D كاملة تُلهي عن الاستخدام**
- Micro-interactions، Page transitions، رسوم بيانية حديثة للمشرف إن أفادت
- طباعة (Typography) حديثة ومتدرجة الأحجام بوضوح

**ممنوع صراحة:** خلفية سوداء بالكامل، تناثر ألوان عشوائية بلا نظام واضح (أيًا كانت اللوحة التي تختارينها)، حركات مفرطة، إهمال إمكانية الوصول (Accessibility) أو حالات التحميل/الفراغ/الخطأ الموجودة أصلًا في كل صفحة — **حافظي عليها، فقط حسّني شكلها**.

---

## 7. المهام الجديدة الحقيقية في هذه المرحلة (غير موجودة إطلاقًا اليوم)

1. **زر تبديل اللغة (عربي/إنجليزي):** أنشئي `src/i18n/en.js` بنفس بنية `ar.js` تمامًا (كل المفاتيح نفسها)، وحالة لغة عامة (Context) تُقرأ من `localStorage`، ودالة `translate()` تختار الملف المناسب. عند التبديل إلى الإنجليزية، بدّلي `dir` على `<html>` من `rtl` إلى `ltr` — ولهذا كل الـ CSS القديم بُني بخصائص منطقية (`margin-inline-start` لا `margin-left`) قدر الإمكان، تحققي من ذلك عند التعديل.
2. **Footer حقيقي:** غير موجود إطلاقًا على أي صفحة اليوم. مطلوب فقط لصفحات الزائر العامة (البوابة، Home، Artisans...): شعار، وصف قصير، روابط (كيف يعمل الموقع، الأسئلة الشائعة إن وُجدت، سياسة الخصوصية)، حقوق النشر. لا تجعليه ضخمًا.
3. **تنقّل واعٍ بالدور (Role-aware navigation) محسَّن:** الشرائط الحالية وظيفية لكنها بسيطة جدًا. حسّنيها لكل دور، وأضيفي قائمة/Drawer للجوال بدل تكديس روابط أفقية.
4. **حالات تحميل أفضل (Skeleton loaders)** بدل نص "جارٍ التحميل..." البسيط الحالي، ونظام إشعارات موحّد (Toast) بدل رسائل النجاح/الخطأ المتفرقة الحالية.
5. **إعادة هيكلة الصفحة الرئيسية (Landing المستقبلية بعد الدخول، أي `/home`)** لتشمل: قسم بحث، خدمات شائعة، "كيف يعمل Fixoria"، حرفيون مميزون، قسم ثقة، دعوة لاتخاذ إجراء.

---

## 8. تحذيرات عملية (تعلّمناها بالتجربة في هذا المشروع)

- **عند تعديل ملف كبير موجود (خصوصًا `src/i18n/ar.js` و`src/App.jsx`)**: امسحي محتوى الملف بالكامل أولًا قبل لصق النسخة الجديدة، لا تُضيفي مقتطفًا وسط الملف — هذا سبّب أخطاء نحوية متكررة أثناء بناء المشروع.
- شغّلي `npm run lint` بعد كل تعديل قبل الانتقال للتالي.
- لا تحذفي أو تعيدي تسمية ملفات موجودة دون تحديث كل مكان يستوردها (`import`)، وتأكدي أن اسم الملف يطابق سطر الاستيراد بالحرف (حساس لحالة الأحرف والجمع/المفرد).
- عند إضافة `en.js`، احرصي أن تطابق بنيته `ar.js` مفتاحًا بمفتاح تمامًا، وإلا ستظهر مفاتيح مفقودة كنص خام على الشاشة بدل الترجمة (سلوك `translate()` الحالي: يعيد المفتاح نفسه إن لم يجده).

---

بالتوفيق! هذا مشروع MVP حقيقي، ركّزي على تحسين ما هو موجود بانسجام بدل إعادة بناء الصفحات من الصفر.
